# Agents

This folder contains specialized subagents for this repository.
Each subagent runs in its own context window and can be assigned a focused task.


## Usage

To add a subagent, create a Markdown file in this folder (for example `code-reviewer.md`) with the subagent definition.
