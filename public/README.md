# public

Static files that Nuxt serves verbatim at the site root, such as `favicon.ico`, `robots.txt`, and social preview images.


## Notes

This is the Nuxt `public/` directory. Unlike files in `app/assets/`, files here are served as-is without any processing, bundling, or hash renaming, so reference them with a root-absolute path such as `/favicon.ico`.
