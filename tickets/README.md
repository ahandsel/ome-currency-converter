# Tickets

Development tickets that needs to be worked on.
For each ticket, please create a branch with the name `ticket/<ticket-number>` (three-digit, for example `ticket/001`) and submit a pull request when the work is done.
Ticket files use the same three-digit prefix (for example `001-folder-structure.md`).
