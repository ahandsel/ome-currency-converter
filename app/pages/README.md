# pages

Route pages for the app. The app is a single page, so this folder holds one file.


## Contents

* [index.vue](index.vue) - the whole interface: topbar, controls column, preview column, and footer. It owns the single wallpaper state and the single rates instance and passes both down to the components as props.
