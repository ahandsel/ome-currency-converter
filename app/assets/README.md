# assets

Assets that the bundler processes, referenced from the app source. Unlike files in `public/`, files here are bundled and hash-renamed at build time.


## Contents

* [css/main.css](css/main.css) - the global stylesheet, registered through the `css` option in [nuxt.config.ts](../../nuxt.config.ts).
